[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "uvicorn api.app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/data/kpis"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
